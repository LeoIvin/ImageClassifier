# ImageClassifier
An image classifier that allows users to upload an image and get a prediction with a confidence rating. It was built with the ResNet50 model that was pretrained on the ImageNet dataset.
